<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Khi ImageView được chọn (selected="true") -->
    <item android:state_selected="true" android:drawable="@drawable/heart_filled_blue" />
    <!-- Khi ImageView không được chọn (mặc định) -->
    <item android:drawable="@drawable/heart_outline" />
</selector>